{
  "name": "slavic-shkaf",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --d1 DB --compatibility-date 2024-01-01",
    "deploy": "wrangler pages deploy public",
    "db:migrate": "wrangler d1 execute slavic-shkaf-db --file=./migrations/0001_init.sql",
    "db:migrate:local": "wrangler d1 execute slavic-shkaf-db --local --file=./migrations/0001_init.sql"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
